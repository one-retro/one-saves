[package]
name = "ps2-memcard"
description = "Read and write PlayStation 2 memory card images: superblock, indirect FAT, directory tree and spare-area ECC"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
categories = ["encoding", "filesystem", "parser-implementations"]
keywords = ["playstation2", "ps2", "memcard", "emulation", "savegame"]
readme = "README.md"
# The integration tests read cards from the workspace's `data/saves`, which is not part of
# this crate. They stay behind rather than shipping a copy of a card per fixture.
exclude = ["tests"]

[dependencies]

[lints]
workspace = true
